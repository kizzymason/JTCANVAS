import OpenPlatformDocsContent from "./docs-content";

/** Public API reference, readable without an account so downstream engineers can evaluate first. */
export default function OpenPlatformDocsPage() {
    return <OpenPlatformDocsContent variant="page" />;
}
