import OpenPlatformDocsContent from "../docs-content";

/** The same reference as `/open/docs`, kept inside the console so reading it is not a context switch. */
export default function OpenConsoleDocsPage() {
    return <OpenPlatformDocsContent variant="embedded" />;
}
