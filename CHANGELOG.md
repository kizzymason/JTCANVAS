# CHANGELOG

## Unreleased

+ [新增] 卡密渠道分销（下游只做前台收银，资金统一进主站）：买家在下游站的组件里下单付款，**订单由主站创建、按主站定价、用主站的支付账户收款、由主站发卡**；下游赚的是佣金，由管理员核对销量后手动分红。主站始终是卖方与收单主体，付给下游的是推广服务费，而不是替它代收货款——这也是这套结构对支付风控最干净的地方。
    - 端点前缀 `/api/card-dist/v1`：`GET /products`（对外售价、库存、预估佣金与可用支付方式）、`POST /checkouts`、`GET /checkouts/{orderNo}`、`GET /orders`、`GET /stats`、`GET /payouts`。刻意没有任何「上报支付成功」「修改金额」「提前放卡」的接口——钱和货都在主站这边。
    - 鉴权：`Authorization: Bearer sk_cd_...`，只存 sha256、常量时间比较，明文仅签发时返回一次，可随时重新签发；支持 IP 白名单与可售商品范围。
    - 幂等：下游自己的订单号即幂等键，`(merchant_id, merchant_reference)` 上的部分唯一索引保证重试返回原订单，不会重复下单、不会让买家付两次。
    - 佣金与钱包同一套纪律：在支付落账的**同一个事务**里计提，`(order_id, type)` 唯一索引保证重复的支付通知不会重复计提；事务内 `SELECT ... FOR UPDATE` 锁行、加减写在 SQL 表达式里、每笔追加 append-only 流水，`CHECK (commission_balance >= 0)` 兜底；每日对账校验「流水合计 == 应付佣金」。
    - 分红：设冻结期（默认 7 天）后才计入「可结算」，退款/拒付先冲回；打款由人工完成后在后台登记，系统不自动转账。已打出的分红遇到退款会记录差额，下次分红时抵扣。
    - 「前端小组件」以可直接复制的参考组件代码交付：它只请求下游自己的后端，不含任何我们的密钥，也不出现主站地址。
+ [新增] 支付风控护栏（针对「多个下游汇入同一个收款账户」这个真实风险点）：
    - **每渠道每日成交额上限**，且未付款的收银台也占用额度——否则可以先开一堆单再同时支付，绕过上限。收款账户单日流水突然放大是最容易触发支付商审核的情形，这是最有效的一道闸。
    - **回跳地址白名单**：付款后跳去哪里只从订单里读（下单时已按后台登记的白名单校验），绝不回显请求里的地址，否则支付回调就成了开放重定向。域名比对按 origin 精确匹配、路径按段匹配，`shop.example.com.evil.test` 与 `/payments` 这类前缀绕过都会被拒。
    - **收银台商品名可按渠道配置**，便于保持中性、不在支付页写「卡密/点卡」这类品类字样。
    - **渠道退款率超限自动停用**（近 30 天成交 ≥ 20 单且退款率 ≥ 20%），保护的是我们自己的收款账户。
    - `notify_url` / `return_url` 始终在主站域名下：网关侧是我们的商户号，回调指向别人的域名正是会被风控盯上的不一致。买家看到的付款页在网关域名下，付完再跳回下游自己的页面，因此**买家浏览器里不出现主站地址**。
+ [变更] 取消原「预付额度 + 提卡 + 下游自行核销」的批发模式。那套模式下下游才是卖方，钱进主站账户反而构成代收；现在改为主站收单、下游拿佣金，定性和实现都随之调整。迁移 `0009` 会删除提卡相关表与预付额度字段，并在仍存在提卡记录或预付余额时**拒绝执行**并提示先结清，避免误删资金记录。
+ [简化] 零售与渠道销售合并为一张订单表：可售库存只需排除 `card_order_items`，`card_order_items.card_id` 的唯一约束就是「一张卡不可能卖两次」的唯一保证，不再需要两套经济体的互斥判断。主站兑换流程随之恢复原样。
+ [修复] 并发购卡会误报「库存不足」：锁卡语句是 `FOR UPDATE SKIP LOCKED`，但没有 `OF redeem_cards`，于是连同 join 进来的批次行一起锁。一个批次通常只有一行，第一个请求锁住它之后，其他并发请求会跳过该批次下的**全部**卡密，明明有货却报缺货。改为只锁卡片行。
+ [新增] 管理后台「卡密售卖」增加「销售渠道」与「对接文档」两个 Tab：渠道增删改与启停、佣金比例或每张固定金额、冻结期、每日成交上限、回跳白名单、收银台商品名、对外售价、IP 白名单、密钥重签发；销量排行（按 7 天 / 30 天 / 全部）、渠道订单（含卡密与退款冲回）、佣金 append-only 台账、手动分红登记与分红记录；以及完整对接文档、错误码表、频率限制、curl / Node / PHP 示例、Webhook 验签片段与参考前端组件。
+ [修复] 零售卡密不再只靠订单号保护：下单时签发 32 字节访问令牌（只存 sha256），`GET /api/card-shop/orders/:orderNo` 只在持令牌时返回卡密——订单号只是时间戳加四字节随机数，熵太低不足以单独作为凭证。邮箱查单作为换设备后的找回通道保留。
+ [新增] `/cards` 与 `/cards/orders` 加入防窥探劝退措施（屏蔽右键、F12、Ctrl+Shift+I/J/C、Ctrl+U，devtools 疑似打开时模糊敏感区），但卡密文本保持可选中可复制。需要说明的是这类前端手段只能劝退不能当安全措施，真正的边界在服务端：没有访问令牌拿不到卡密、没有商户密钥提不到卡。
+ [新增] 开放平台（API 代理商）：以 OpenAI 兼容协议对外开放 PiAPI / WhatsToken 渠道的图片、视频与文本模型，对外 Base URL 为 `https://<host>/v1`，下游改一个 baseURL 即可用官方 SDK 接入。
    - 端点：`GET /v1/models`、`POST /v1/images/generations`（同步阻塞）、`POST /v1/videos` + `GET /v1/videos/{id}` + `GET /v1/videos/{id}/content`（异步三件套）、`POST /v1/chat/completions`（流式与非流式）。
    - 鉴权：`Authorization: Bearer sk-jt-...`，只存 sha256，明文仅创建时返回一次；支持按令牌配置额度上限、RPM、并发、模型白名单、IP 白名单与过期时间。
    - 错误与限流：统一 OpenAI 错误信封 `{ error: { message, type, code, param } }`，429 带 `Retry-After` 与 `x-ratelimit-*` 配额头。
    - 调用明细默认保留 90 天（`OPENAPI_LOG_RETENTION_DAYS`）后由每日维护任务清理，日汇总永久保存。
+ [修复] 视频异步任务的令牌额度与调用日志改为在 worker 结算时一次性落账：反复轮询不再重复累计额度，从不轮询也不会漏记金额，一个任务恒定只有一条日志。
+ [修复] 令牌额度改为提交时预留、结算时按实扣冲正，失败任务全额释放；额度耗尽时提交直接返回 `insufficient_quota`，不再出现超额消费后才拦截。
+ [新增] 管理后台用户管理可直接把角色改为「代理商」，自动按默认等级建档并开通控制台；改回普通用户会同步停用代理商资格与其令牌。降级最后一个管理员会被拒绝。
+ [修复] `/v1` 对外错误信息统一为英文，不再把「生成任务不存在」等站内中文提示透出给下游 SDK。
+ [修复] 限流 429 补齐 `Retry-After` 与 `x-ratelimit-*` 响应头，并发超限单独返回 `concurrency_limit_exceeded`。
+ [修复] nginx 改为只信任内网反代还原真实客户端 IP，令牌的 IP 白名单不再可被伪造的 `X-Forwarded-For` 绕过。
+ [新增] 卡密售卖页（私域直达链接，不在前台导航中出现）：`/cards` 免登录选购并付款，付款后当页直接拿到卡密；`/cards/orders` 用下单邮箱即可查回历史订单与卡密，不与账号绑定。买到的卡密就是普通兑换卡，登录后在「我的钱包 - 卡密兑换」自行输入到账。
+ [新增] 管理后台「卡密售卖」：配置商品名称、面值与售价、单次购买上限与上下架，按商品分类导入已有卡密或直接生成新卡密（重复卡密自动跳过），并查看销售订单、已发放卡密与收款汇总。支付沿用现有的支付渠道配置，无需另行接入。前台模型选择器、Agent 输入框与开放平台模型列表改为显示厂商品牌图标（OpenAI、Claude、Gemini、DeepSeek、MiniMax、火山引擎、通义、可灵、智谱、Grok 等），按模型名识别，识别不出的才回退到原来的芯片图标。图标取自 @lobehub/icons-static-svg（MIT）并随仓库分发，不引入运行时依赖。
+ [修复] 按 Token 计费的文本模型不再少收：冻结时在本地估算的输入 token 上预留 512 的余量以覆盖上游自带的系统框架开销（实测本地算 7 个而上游计 118 个），结算按上游真实用量扣费，多冻的部分照常退回。
+ [新增] WhatsToken 渠道接入 14 个模型：视频 MiniMax-H3 与 MiniMax-H3-special、HappyHorse 1.1 三件套（按时长计费）；图片 GPT Image 2 三个变体与 Gemini 3.1 Flash Lite Image；文本 DeepSeek V4 Flash 两档、GPT-5.6 Sol 两档与 Seed SC（按 token 计费）。上游暂无可用渠道的 7 个模型以「禁用」状态入库，恢复后在后台一键启用。
+ [新增] 支持按时长计费的视频模型：区别于 Seedance 的编码 token 公式，MiniMax / HappyHorse 直接按秒×清晰度计价，并按各自的分辨率词表（如 MiniMax 只认 `768P` / `2k`）与必填宽高比请求上游。
+ [新增] GPT Image 2 按上游实测的出图 token 数折算分档单价（1024×1024 的 low/medium/high 分别是 196 / 1756 / 7024 输出 token），对应 1K/2K/4K 三档；不选档位时按最高档计费，避免亏损。
+ [修复] 视频结果解析支持 `data: [{ url }]` 列表形态，MiniMax 这类上游不再出现「任务完成但没有返回文件」。
+ [调整] 顶栏「开放平台」图标由插头换成原子轨道（atomic-orbits）。
+ [修复] 反色提示卡的标题仍是深色、压在黑底上看不清：antd 6 把标题类名从 `ant-alert-message` 改成了 `ant-alert-title`，样式改为同时覆盖两者，并把标题加粗。
+ [调整] 令牌管理的创建/编辑改为从右侧划出的抽屉，不再用居中弹窗——表单较长时过期时间、IP 白名单等字段不用缩小窗口才能看到。
+ [新增] 控制台内置 API 文档页（`/open/console/docs`），内容与前台 `/open/docs` 一致，左侧菜单不再跳出到前台。
+ [修复] 深浅色主题下的「信息」提示卡恢复为反色卡片（浅色黑底白字、深色白底黑字），卡片内的说明文字与可复制内容不再沿用自身灰色而导致看不清。
+ [调整] 开放平台入驻申请由页面内长表单改为「提交申请」按钮 + 弹窗填写；被驳回后再次打开会带出上次填写的内容。
+ [新增] 管理员默认具备开放平台权限：无需入驻申请即可进入控制台、创建令牌并调用 `/v1`，按公开价计费，方便自测与排障。
+ [修复] 开放平台落地页与 API 文档页无法滚动、页面像卡死：这两页缺少自己的滚动容器，被外层布局裁掉了超出屏幕的内容；文档页右侧锚点目录同时改为跟随页面实际的滚动容器。
+ [新增] 开放平台控制台（顶栏「我的资产」右侧入口）：仪表盘（今日 Token / 预估成本 / 请求数 / 总令牌 + QPS/RPM/TPM/Task 实时吞吐 + 最近活动 + 模型成本分布）、令牌管理、日志记录（筛选与 CSV 导出）、模型列表（含倍率后价格）、个人中心（等级、钱包与充值）。
+ [新增] 开放平台落地页与入驻申请（`/open`，含审批状态与驳回原因回显）以及公开 API 文档页（`/open/docs`，含 Base URL、鉴权、端点一览、curl 与 SDK 示例、错误码表、限流与计费说明）。
+ [新增] 管理后台增加代理商管理与入驻审批（通过后自动把用户角色改为代理商）、代理商等级与倍率管理；等级倍率按「代理商单价 = 公开售价 × (1 + 加价幅度)」生效，加价幅度可为负数表示优惠，优先级为专属倍率 > 等级倍率 > 原价。
+ [新增] 服务管理增加「开放平台」开关，关闭后隐藏入口与控制台，已签发令牌同时停止服务。
+ [新增] 文本模型支持按 Token 计费（价格行 `spec=input/output`，单位为元/1M tokens）：提交时用 tiktoken 估算输入 tokens 并以 `max_completion_tokens` 作为输出上限预扣，结算按上游返回的真实用量核销并封顶在预扣金额。
+ [修复] 视频按 token 结算不再绕过代理商倍率：`seedanceTokenSell` 的结果先乘倍率再与按秒 pro-rate 比较，避免优惠代理商被多扣、加价代理商被少扣。
+ [调整] 前台顶栏导航去掉底部黑线，选中改为轻量滑动高亮，悬停用缓动过渡。
+ [调整] 桌面顶栏导航略加大：高度 80px，图标 22px，文字 14px。
+ [调整] 窄屏顶栏钱包只显示图标，不再把余额数字挤到其它按钮上。
+ [调整] 登录弹窗品牌改为较大的 JTCANVAS，logo 随标题字号适配，并去掉登录说明文案。
+ [调整] 前台导航改用 Nucleo Glass 图标并收紧桌面字号与间距；「生图工作台」改为「图片生成」，「视频创作台」改为「视频创作」。
+ [调整] 仪表盘生产类型与访客站点页面排行去掉橙色悬停，条形图改为静态展示。
+ [修复] 管理后台折线图十字准星按 SVG 坐标对齐到当日数据点，不再相对鼠标偏移。
+ [优化] 仪表盘与访客统计在数据返回前显示与其它后台页一致的居中加载态。
+ [调整] 前台顶栏品牌改为紧凑的 JTCANVAS，避免挤占导航。
+ [调整] 登录注册弹窗去掉底部提示，注册按钮改为「注册」。
+ [修复] 管理后台仪表盘改为读取真实用户、钱包与任务数据，并用图表展示近 14 日趋势。
+ [修复] 管理后台概览接口不再因把 Date 直接写入 SQL 而 500。
+ [优化] 管理后台图表支持十字准星与数据提示，饼图区分对比色；仪表盘 KPI 改为「累计流水」。
+ [调整] 访客统计改为图表展示，路径只统计首页、画布库、生图、视频与资产，不再列出私人画布。
+ [优化] 管理后台侧栏隐藏滚动条，并加强边框与阴影。

+ [修复] Seedance 图生视频拒绝 4 秒以下的时长（wljjj 的 1 秒/2 秒会被上游 r2v 直接 400），设置项下限改为 4 秒。
+ [修复] 发版后未刷新的标签再点路由，过期 JS 分包不再白屏，改为提示「页面已更新，请刷新页面」。
+ [修复] 视频按秒计费不再把秒数当成条数：一条成功视频按完整时长结算，管理记录显示为成功而不是 1/12 部分成功。
+ [修复] Seedance 按秒售价按上游 token 量（编码像素 × 帧）× 7.2 × 1.3 校正；480p/720p 分开计费；成功任务若返回 token 则按 token 结算并封顶预扣。
+ [调整] 视频秒数预设改为 5/10/15；生成声音默认开启且不另外计费，版权失败时可关闭后重试。
+ [修复] 生图工作台窄屏下长模型名不再把页面撑出横向滚动，「调整」按钮保持在行内。
+ [调整] 注册弹窗去掉滑块验证，防刷改为本机设备 ID 指纹与 IP 限制，避免部分用户滑不动或被误伤。
+ [优化] 访客上报延后到页面 load 后空闲执行、接口立即返回且不查会话，第三方统计脚本同样延后，首页标题去掉会卡住移动端的投影动画。
+ [新增] 管理后台增加支付渠道与充值套餐管理，前台钱包可按套餐或自定义金额走 Z-Pay / 易支付（支付宝）充值到账。

+ [新增] 管理后台运营组增加访客统计：前台页面 PV/UV 与明细，明细保留 30 天，日汇总永久保存。
+ [新增] 注册增加滑块验证与设备指纹限制（同一设备 365 天只能成功注册一次；同一 IP 24 小时最多 5 次）。
+ [修复] 卡密兑换失败统一提示并不再暴露卡密是否存在，失败次数过多会进入冷却。

+ [修复] Seedance 视频改为调用 WhatsToken 的 `POST /v1/video/generations`（不再误打 Sora 的 `/v1/videos`），上游控制台能看到请求记录。
+ [修复] 生图 1K/2K/4K 计费与上游档位对齐：按所选质量档和官方像素/面积计费，不再用最长边 1280/2560 或 1K 翻倍推断价格（避免 16:9 的 1424x800 被打成 2K、2816x1584 被打成 4K）。
+ [调整] 生图默认宽高比改为主流平台原生像素（Seedream 16:9 为 1424x800 / 2816x1584 / 5504x3040），2K 不再一律按 1K 翻倍；视频预设仍用 1280x720。计费按所选 1K/2K/4K 档位而不是最长边。
+ [调整] PiAPI Seedream Pro/Lite 宽高比像素与官方 Seedream 一致，不再使用文档里的 1312x736 单独表。
+ [新增] 管理后台可按模型增删改宽高比及 1K/2K/4K 像素，各模型互不影响。
+ [修复] PiAPI 图生图支持参考图：提交接口允许公网 https 地址（不再要求必须是本账号 storageKey）；公网地址直接透传 `image_urls`；本地上传由 worker 转到 PiAPI 临时图床，套餐不够时再用短时签名的站点文件地址，最多 10 张。蒙版编辑仍不支持。
+ [调整] 生图工作台剪切板除图片文件外，也可粘贴公网图片 URL 作为参考图，不经本地再上传。
+ [修复] PiAPI 渠道在已有模型后，重启或部署不再补种 Seedream 5 Pro/Lite 预设，也不再改写管理员已配置的展示名和价格。
+ [新增] 接入 WhatsToken 正价渠道，预置 Seedream 生图与 Seedance 生视频模型；售价为上游美元价加价 30% 后按 7.2 折算人民币，视频预估按是否包含参考视频自动选择含视/无视单价。


+ [调整] 未登录访问画布等入口改为首页弹出登录窗，不再跳转到独立登录页。
+ [优化] 登录与注册滑块与轨道留出间距，切换改为更顺的弹簧动效。
+ [调整] 首次访问默认使用浅色主题。

+ [调整] README 改为 JTCANVAS 商业化产品说明，去掉原 infinite-canvas 开源仓库的介绍与外链。

+ [调整] 首页主标题改为 JTCANVAS，标语改回「无限画布」；画布库页只保留「画布库」标题，新建画布默认命名为「画布1」。
+ [调整] 网站标题改为「景甜Canvas AI创作画布」；管理后台站点名称只同步管理端顶栏和浏览器页签。
+ [调整] 画布空白处改为右键打开紧凑的「创建节点」菜单，不再通过双击创建。

+ [调整] 我的钱包里余额流水、使用记录、充值订单和账号安全二级抽屉改为约半宽，不再铺满整屏。
+ [新增] 管理后台用户管理支持批量选择并删除用户，卡密管理支持筛选、多选和批量删除。
+ [新增] 财务流水与生成记录支持筛选、搜索和导出 CSV。
+ [修复] 仪表盘对账不一致提示可关闭，并可查看具体钱包明细。
+ [修复] 存储管理提示改为黑底白字，浅色主题下不再看不清。
+ [修复] 管理后台左侧导航在条目较多时可滚动，并能点到最下方菜单；「返回前台」改到顶栏用户名旁，侧栏不再提供收起按钮。
+ [新增] 管理后台新增服务管理，可开关图片生成、视频生成和 Agent 前台入口，默认全部开启。
+ [优化] 我的钱包抽屉略加宽，余额卡片改为更紧凑的银行卡比例，并修正充值按钮对比度与充值滑块内边距。
+ [调整] 个人中心改为「我的钱包」窄抽屉：卡片上提供充值/提现居中弹窗，流水、使用记录、订单和账号安全在二级全宽抽屉中打开。
+ [修复] 手动加入我的资产不再因请求体含未允许字段而 400，图片会按 storageKey 绑定到已上传文件。
+ [调整] 个人中心抽屉余额区改为银行卡式卡片，仅展示钱包余额。
+ [调整] 登录注册切换改为小圆角矩形滑块。
+ [调整] 画布库默认隐藏「删除全部」和卡片勾选框；点击「选择画布」后才显示删除全部、导出选中和删除选中，并隐藏导入与新建；删除全部为黑底按钮，完成为深绿色。
+ [优化] 画布库卡片改为更小的方形黑白渐变盒子，并加入克制的悬停光泽。
+ [新增] 管理后台可按模型配置生图分辨率、宽高比、单次张数和透明背景，以及视频清晰度与最长秒数。
+ [调整] 画布和工作台的图像设置将宽高比与分辨率分开，生成张数改为可输入的下拉框，并按所选模型隐藏不支持的选项。
+ [调整] 暂时隐藏画布顶栏「节点插件」和底部「扩展节点」入口，插件相关代码保留待后续开放。
+ [调整] 模型下拉不再附带单价，生成按钮旁的预计费用改为闪电图标。
+ [修复] 补全画布参考栏、选择参考提示、视频截帧和空节点相关中英文文案，不再显示翻译键。
+ [调整] 个人中心改为右侧抽屉：点击余额即可在画布和工作台查看充值、流水与消耗，顶栏不再单独放个人中心导航。
+ [调整] 偏好设置改为从右侧划出的抽屉，不再使用居中弹窗。
+ [调整] 前台顶栏不再提供管理后台入口，管理员直接访问 `/admin`。
+ [优化] 用户前台顶栏加高，导航与操作区的图标、文字一并加大。
+ [调整] 顶栏「个人中心」改为用户图标，余额入口继续使用钱包图标。
+ [调整] 首页与画布的 Agent 入口改由管理后台服务管理控制，默认开启；关闭后隐藏相关入口，Agent 代码保留。
+ [调整] 首页与画布顶栏去掉文档、版本号和 GitHub 入口，界面语言改到偏好设置中切换。
+ [修复] 管理后台顶栏改为与页面一致的浅色底与深色文字，点击用户名不再跳转。
+ [修复] 本地开发打开生成图片不再返回 0 字节空文件；没有 nginx 时由 API 直接输出磁盘内容。
+ [修复] 恢复 PiAPI 渠道与四个 Seedream 生图模型，服务启动时自动预置人民币单价，生图默认使用 Seedream 5 Pro 宽松。
+ [修复] 画布与素材列表按 200 条分页拉取时不再被服务端 `pageSize` 上限拦截，列表失败时也不再无限转圈。
+ [修复] 提升用户为管理员后，当前登录会话立即按数据库角色生效，右上角会显示管理后台入口。
+ [调整] 接口文档不再公开挂载 `/api/docs`，改到管理后台「接口文档」页，需管理员登录后查看。
+ [新增] 新增用户前台个人中心：余额与冻结、卡密兑换、余额流水、使用记录、充值订单与修改密码，并支持把浏览器里的旧数据一次性导入云端。
+ [新增] 新增左侧边栏风格的管理后台 `/admin`：仪表盘、用户管理、渠道与模型、价格与计费方式、财务流水与对账、卡密管理、生成记录、存储管理、PiAPI 账号池、站点设置与审计日志。
+ [新增] 生图工作台、视频创作台与画布提示词面板在生成按钮旁显示预估价格，余额不足时禁用并引导充值。
+ [调整] 全站改为登录后使用，未登录只能访问首页与登录注册页；登录与注册合并为同一页面并用滑块切换。
+ [调整] 画布、素材与生成结果文件全部改为服务端存储，登录同一账号后跨设备可用；画布保存改为按项目增量保存并用版本号做乐观锁。
+ [调整] 第三方 API Key 与自定义调用脚本移到服务端由管理员配置，前台不再持有任何密钥；渠道配置、model script 编辑器、WebDAV 面板、本地存储面板与 `/piapi` 页面从前台移除。
+ [调整] 存储策略可在管理后台切换本机磁盘与 S3 兼容对象存储，并自动生成缩略图供列表与画布使用。
+ [调整] 部署形态由单个静态站点变为 nginx + api + worker + postgres + redis 组合，`docker-compose.yml` 与部署文档同步重写。

## v0.17.0 - 2026-08-31

+ [新增] 接入 PiAPI 渠道与 Seedream 5 系列生图模型，并新增 PiAPI 多账号 API 池管理页面，支持 CSV 导入账号、监控余额，在账号余额耗尽时自动切换到下一个账号。

+ [调整] 移除提示词中心、提示词库及提示词来源，首页、导航、配置、画布和工作台不再提供该能力。

+ [修复] 文档站默认英文路径不再因内部语言重写产生重定向循环。
+ [优化] 文档站移动端折叠菜单新增分类切换入口，桌面端增加随滚动高亮的本页目录。
+ [优化] 画布左侧元素列表按组展示树形层级，组内节点支持展开和收起。
+ [优化] Canvas Agent 生成流程复用仅由现有节点引用构成的提示词，避免创建重复文本节点。
+ [新增] 节点输入框上方新增参考内容栏，可预览、移除或从画布连续选择参考节点；组引用会展开组内全部内容，移除任一项即断开整组。
+ [新增] 视频节点右键菜单支持截取首帧、尾帧和当前帧并生成图片节点，方便衔接连续镜头。
+ [调整] 移除文本节点右上角的生图按钮，让正文区域保持简洁完整。
+ [调整] 多次文本生成合并为单个文本节点，其他结果作为可展开切换的备选文本。
+ [新增] 组节点可作为生成输入并通过单条连线或 `@` 一次性引用组内全部有效资源。
+ [修复] 透明图片在多图备选展开时保持透明背景，不再显示白色卡片底色。
+ [修复] Canvas Agent 转发 CLI 错误日志前脱敏常见凭据，避免调试日志和浏览器事件流泄露密钥。
+ [修复] Canvas Agent 配置目录和配置文件收紧访问权限，降低本地连接 Token 被其他系统用户读取的风险。
+ [修复] Canvas Agent 自动连接凭据改用 URL fragment 传递并在读取后立即清除，避免 Token 进入服务器日志、Referer 和浏览器历史。
+ [修复] GPT Image 模型请求不再发送不受支持的 `response_format` 参数。
+ [修复] 图片接口返回临时外链时统一下载、校验并本地保存，支持取消下载，跨域无法读取时保留可显示的原链接。

## v0.16.0 - 2026-08-18

+ [新增] 提示词来源新增 Freestylefly GPT Image 2 内置来源。
+ [调整] 移除火山方舟协议及 Seedance 专用适配。
+ [调整] 画布默认使用移动工具，可直接拖动元素或空白画布，按住 Control 可临时框选节点。
+ [调整] 双击多图节点统一放大预览图片，仅通过右上角张数按钮展开或收起图片组。
+ [修复] 文本节点和生成配置新增独立的文本生成次数，默认一次且不再误用生图数量。
+ [修复] 节点缩放期间暂停渲染提示词面板，避免导入图片后立即缩放导致页面崩溃。
+ [优化] 生图、视频和音频请求遇到跨域拦截时明确提示需要通过自己的服务转发。
+ [优化] 图片工具条默认仅显示图标并移除图片、文本编辑入口，单击图片、文本或视频节点直接打开下方提示词面板。
+ [优化] 画布多图展开状态改为由用户主动控制，操作画布其他区域时不再自动收起。
+ [优化] 展开的多图子图支持双击直接放大预览，无需先设为主图。

## v0.15.1 - 2026-08-07

+ [调整] 项目开源协议更换为MIT，允许所有人免费用于开源、闭源和商业场景。

## v0.15.0 - 2026-08-07

+ [新增] 画布节点提示词支持弹窗放大编辑和实时同步。
+ [新增] 配置页新增本地存储、站点配额与对象仓库占用信息。
+ [新增] 画布多图支持失败项重试与删除、创建副本和单图下载。
+ [新增] Agent 输入框支持通过 `/` 快速选择 Skill，并通过 `@` 将画布素材按光标位置插入正文，输入及发送后均支持悬浮预览。
+ [优化] 画布新增选择与移动模式，并支持 Control 或空格临时反转工具。
+ [优化] 画布多图合并为可展开的单节点，支持实时状态、主图切换和四列布局。
+ [优化] 从左侧元素列表定位节点时，自动缩放上限调整为 100%。
+ [优化] Agent 用户消息元数据改为独立的版本化文件存储，刷新或重启后仍可恢复附件、Skill、画布引用及本地缩略图，未知存储格式不会被覆盖。
+ [修复] 关闭输入面板后保留图片与文本节点的编辑提示词。
+ [修复] 点击画布空白处时同步收起节点工具条和输入面板。
+ [修复] 空格键切换工具时不再误触当前聚焦的按钮。
+ [修复] 清理本地图片时保留工作台生成历史引用的文件。
+ [修复] 修复画布多图切换主图、重试和调整尺寸时的缩放与状态异常。
+ [修复] 统一画布多图操作按钮在不同主题下的颜色与对比度。

## v0.14.0 - 2026-08-05

+ [新增] 文档站支持英文默认与简体中文切换，正文、导航、搜索及 LLM 文档接口按语言独立展示。
+ [新增] 前端搭建中英文国际化框架，支持从右上角一键持久化切换全局导航、首页、配置中心、Ant Design 和日期组件语言。
+ [修复] 统一 Ant Design 下拉菜单与选择器在浅色、深色主题下的弹层及交互状态颜色。
+ [修复] Agent 顶栏在英文模式或可用宽度不足时自适应隐藏标签文字，避免内容被裁切。
+ [新增] Agent 支持查看、创建、编辑、删除、启停和显式调用本地 Codex Skill，并同步多标签页变更。
+ [新增] Agent 支持从当前对话或当前画布生成可编辑的 Skill 草稿，并在用户确认后保存。
+ [调整] Agent 面板连接状态、内容标签和新对话统一排版，窄面板仅隐藏文字并保留图标与数量。
+ [优化] Agent 输入区控件随面板宽度在图标与图标加文字之间自适应。
+ [修复] Agent Skill 草稿期间保持 MCP 活动画布隔离，支持停止临时生成任务，并禁止并发修改 Skill。
+ [修复] Agent 新建或恢复对话时统一同步版本化会话与 MCP 初始化状态，避免多页面竞态误触 `409`、提前发送或任务后残留初始化卡片。
+ [优化] 提示词中心缩小卡片、每行展示四张，移除“加入我的资产”按钮边框。
+ [优化] 缩小生图与视频工作台的提示词库弹窗，精简卡片。

## v0.13.0 - 2026-08-03

+ [优化] Agent 进入空白对话或点击新对话后后台预热 Codex 与 MCP。
+ [优化] Agent 排查日志改为筛选栏结构化滚动列表，支持筛选、展开详情并折叠连续重复事件。
+ [优化] Agent 日志统一使用居中的回到底部入口，向上浏览时暂停跟随并可快速返回最新内容。
+ [优化] Agent 对话将同一轮连续命令合并为按数量折叠的命令组，默认隐藏冗长命令预览。
+ [修复] MCP 初始化期间禁用 Agent 按钮与回车发送，避免首条消息提前清除服务加载状态。
+ [修复] Agent 对话代码块关闭行号后多行内容被拼接为一行的问题。
+ [修复] 统一 Agent 实时事件与线程历史的消息归属，避免多窗口或刷新后出现过程重复等问题。
+ [修复] 节点缩放期间暂停渲染跟随节点的浮层工具条高频更新导致页面崩溃。

## v0.12.1 - 2026-07-31

+ [新增] 画布右侧Agent支持按当前账号可用范围选择Codex 模型与推理强度。
+ [优化] Canvas Agent 升级至最新 Codex，启动时检查版本更新，
+ [优化] 统一通过公共日志输出 Info 以上信息，美化转发日志时间。
+ [优化] Agent高速流式回复取消逐词动画排队和当前消息离屏占位。
+ [修复] Agent首次发送消息时立即保留并展示用户内容，不再晚于思考状态。

## v0.12.0 - 2026-07-30

+ [新增] 画布 Agent 支持三档 Codex 权限，并可在对话中审批操作。
+ [新增] Canvas Agent 支持 `--debug` 日志并按日期保存。
+ [新增] Agent 新增可折叠任务进度，实时显示各步骤处理状态。
+ [调整] Agent 工具确认模式移至输入框并默认自动确认。
+ [调整] 进入画布后默认开启空白对话，历史对话改为主动恢复。
+ [修复] Agent 默认通过节点生成，明确指定时才使用 Codex 内置生图。
+ [修复] Agent 本地路径改为在文件管理器定位，不再拼接 localhost。
+ [修复] Agent 启用 Codex 思考摘要，并修复历史恢复后过程记录缺失。
+ [修复] 修复 Agent 图片预览打开后产生额外空行的问题。
+ [修复] Canvas Agent 停止任务仅中断 turn，不终止服务进程。
+ [修复] Agent 修复运行提示闪退和回复需切换标签页才显示的问题。
+ [修复] Agent 任务失败时立即结束等待，并在对话和历史中显示中文原因。
+ [修复] Agent 修复图片附件偏移，历史恢复保留缩略图且不回显内部说明。
+ [修复] 修复画布复制快捷键拦截 Agent 对话和节点详情文本复制。
+ [优化] Agent「新对话」立即清空并后台同步，不再等待创建 Codex 线程。
+ [优化] Agent「读取画布」按节点类型和连线分类展示内容概览。
+ [优化] Agent 发送后立即清空输入框并展示消息，避免内容滞留或草稿被清空。
+ [优化] Agent 动态工具卡片显示名称、状态和失败原因，替代「工具操作」。
+ [优化] Agent 面板标题、功能标签和新对话操作合并为单行顶栏。
+ [优化] Agent Markdown 代码块与外链确认弹窗改为紧凑中文样式。
+ [优化] Agent 思考摘要支持 Markdown 渲染，改善列表和代码阅读。
+ [优化] Agent 思考摘要和命令改为无边框折叠行，保留实时返回内容。
+ [优化] Agent 用户图片改为紧凑缩略图并支持单击放大。
+ [优化] Canvas Agent 的 Codex CLI 升级至 0.145.0。
+ [优化] Canvas Agent Debug 日志改为纯文本单行格式。
+ [优化] 精简 Agent HTTP 诊断日志，过滤轮询、流式重复事件和原始响应。
+ [优化] Agent 对话移除头像，工具活动紧凑展示，Token 用量支持数字动画。
+ [优化] Canvas Agent 流式回复改为增量传输，减少长对话重复渲染。
+ [优化] Agent 新增过程时间线，工具卡片改为中文摘要和可读详情。
+ [优化] Agent 长时间等待时显示阶段、时长和停止提示，避免空会话读取失败。
+ [优化] Agent 历史记录支持多选批量删除，点击记录可直接进入对话。
+ [优化] Agent 优先操作当前画布，不再无故查询列表或重复导航。
+ [优化] Canvas Agent 独立维护工作区指令，不再重复拼接提示词。
+ [优化] Canvas Agent 目录按 Agent、画布、服务和工具拆分职责。

## v0.11.0 - 2026-07-28

+ [新增] 画布文本生成新增推理强度设置，支持默认调用与自定义调用脚本读取所选档位。
+ [新增] 生图与视频工作台的参考资产区域支持直接拖动文件上传。
+ [新增] 配置与用户偏好支持通过配置文件导入和导出。
+ [新增] 模型渠道新增火山方舟协议。
+ [优化] 画布左侧元素列表新增图片放大预览操作。
+ [优化] 图片遮罩、切图与裁剪编辑支持常用快捷操作并修复高分辨率图片预览闪烁。
+ [优化] 提示词详情弹窗改为上下布局并限制显示尺寸。
+ [修复] 画布组装提示词浮层限制正文高度，长提示词改为区域内滚动查看。
+ [修复] 修复画布节点提示词过长时无法使用鼠标滚轮滚动的问题。
+ [修复] 修复画布重试时组装提示词重复以及切换类型调用模型不一致的问题。
+ [修复] 修复生成完成后再次选中画布节点时提示词不再回显的问题。

## v0.10.0 - 2026-07-25

+ [新增] 提示词来源新增BananaPromptQuicker，并支持添加自定义标准 JSON 来源。
+ [新增] Agent 新增统一状态查询，支持查看画布、生图工作台和视频工作台任务进度。
+ [调整] 内置提示词来源改为读取ImagePrompts 统一JSON 数据，不再由画布端分别解析。
+ [调整] 提示词来源配置保留卡片式交互，并展示来源数量、同步状态和上次成功时间。
+ [优化] 提示词来源支持独立缓存和更新，更新失败时保留上一次成功内容。
+ [优化] Agent 对话区分用户与 AI 消息，并优化会话切换和回到最新消息的交互。
+ [优化] 画布生成完成后保留提示词输入，并改善提示词库和资产面板的交互性能。
+ [优化] 提示词中心标题居中、搜索输入增加防抖，并采用左侧筛选、右侧内容的双栏布局。
+ [修复] 本地 Agent 完善多标签页请求隔离、结果归属、焦点回退和 Codex 会话状态同步。
+ [修复] 本地 Agent 上传的图片附件可正确创建画布图片节点并连接生成流程。
+ [修复] 画布提示词库支持跨来源搜索，插入节点时保留提示词标题。

## v0.9.0 - 2026-07-17

+ [新增] 左侧面板「资产」Tab 支持上传添加图片/视频资产、卡片悬停移除资产。
+ [新增] 左侧画布面板支持拖拽调整宽度、展开/收起(带动画),顶栏菜单左侧新增面板开关按钮。
+ [新增] 顶栏菜单新增「导出当前画布」,导出为包含全部资源的压缩包。
+ [新增] 左侧画布元素列表支持多选并批量导出选中元素为压缩包。
+ [新增] 可选的网站统计分析:支持 Google Analytics 4 与百度统计。
+ [调整] 画布节点名称默认不再显示,仅在选中/悬停/编辑时出现,画布更简洁。
+ [优化] 左侧面板「画布/资产」切换改为带滑动下划线的动画,移除非图片元素图标的灰色底色。
+ [优化] 画布节点提示词面板 `@` 引用图片时,输入框内直接显示真实缩略图。
+ [优化] 移除画布节点右上角的「图片1/文本1」资源角标,引用改在对话面板 `@` 直接选取。
+ [修复] 连接本地 Codex Agent 后,拖拽画布节点边框缩放等高频编辑导致页面崩溃。

## v0.8.2 - 2026-07-16

+ [新增] 图像设置新增「透明背景」开关,开启后生成无背景的透明图像。
+ [优化] 画布节点输入区域移除灰色底色与边框、美化样式。
+ [修复] 画布节点提示词输入框补上悬停文本光标。

## v0.8.1 - 2026-07-16

+ [新增] 插件 SDK 扩展:AI 生成能力、面板控制能力。
+ [优化] 3D 全景(1.1.0)支持上传与 AI 生成并升级查看器;
+ [优化] HTML 节点(1.2.0)迁移到统一交互开关;
+ [优化] 便利贴(1.1.0)可拖动移动、自选颜色、移除资源角标与衍生功能;
+ [优化] SVG 节点(1.1.0)透明背景融入画布、可拖动、去除默认值;
+ [优化] 画布节点渲染性能:memo 化节点回调,交互时不再全量重渲染;
+ [修复] 修复 Markdown 节点在点击/移动视角时重复渲染导致图片反复请求的问题;
+ [修复] 修复插件版本号显示不更新，插件面板新增可升级绿点提醒。

## v0.8.0 - 2026-07-15

+ [新增] 画布节点插件系统:支持通过 URL 动态安装/启用/更新/卸载远程节点插件。
+ [新增] 插件开发 SDK,可用 TypeScript 开发画布节点插件。
+ [新增] 新增 Markdown、SVG、HTML、3D 全景、便利贴等示例插件。
+ [新增] 官方插件注册表:节点插件面板可从项目仓库读取官方插件列表并一键安装。
+ [新增] 在画布右上角工具栏新增「节点插件」入口。
+ [新增] 支持自定义生图/视频接口调用方式以适配不同中转站。

## v0.7.1 - 2026-07-15

+ [修复] 修复页面白屏报错的问题。

## v0.7.0 - 2026-07-14

+ [新增] Agent 对话消息改用 streamdown 流式渲染，提升Markdown 内容展示效果。
+ [新增] Agent 新增画布、工作台、提示词库和素材等站点级工具。
+ [新增] Agent 面板改为全站常驻右侧栏，开关时同步推动顶栏和页面内容。
+ [新增] Agent 新增 `site_navigate` 工具，支持页面跳转。
+ [新增] Agent 对话运行中支持一键停止，中断当前 Codex turn。
+ [新增] 画布节点支持统一维护名称字段，默认显示在节点上方，并可直接双击名称编辑。
+ [新增] 画布新增组节点，支持节点拖入/拆出分组、拖拽高亮吸附和移动组时带动子节点。
+ [新增] 画布空白区域支持双击打开节点选择菜单，并在点击位置创建节点。
+ [调整] Codex 会话改为站点级连续线程，跨页面和跨画布保持同一上下文。
+ [调整] 移除仅前端调用 OpenAI responses 接口，统一走 MCP + 本地 Codex 链路。
+ [调整] 画布节点顶部工具条改为点击选中节点后显示，避免鼠标经过节点时频繁弹出。
+ [优化] 本地 Agent 连接说明明确区分插件 / 手动 MCP 才会增加 Codex token 消耗。
+ [优化] 优化本地 Agent 连接说明，区分 Codex 插件启动和直接运行 Agent 两种方式。
+ [修复] 修复 Gemini 格式生图时因内置比例列表触发误报导致生成失败的问题。

## v0.6.0 - 2026-07-09

+ [新增] 新增Codex App插件支持。
+ [新增] 配置与用户偏好新增独立页面和 Codex 连接配置 Tab。
+ [新增] 新增GitHub Pages 前端静态站点发布 workflow。
+ [新增] 图片切图支持等分线直接拖拽调整，并可新增、删除和重置横向 / 纵向切图线。
+ [调整] Docker 运行镜像改为 nginx 静态托管。
+ [调整] 移除网站Agent模式，专注于连接Codex Agent操作画布
+ [修复] 修复生图工作台重试成功结果刷新后丢失的问题。
+ [修复] 修复 Gemini 调用格式生图未传递尺寸比例配置的问题。
+ [修复] 修复前端 TypeScript 构建报错。
+ [修复] 修复画布生成配置切换文本/视频/音频模式时模型仍显示为生图模型的问题。
+ [修复] 兼容中转站视频任务直接返回视频 URL 且没有 `/content` 接口的情况，并优化失败原因展示。

## v0.5.0 - 2026-07-05

+ [新增] 渠道兼容Gemini格式。
+ [调整] 前端从 Next.js 迁移到 Vite，项目改为静态前端构建。
+ [调整] 移除已 404 的 EvoLinkAI 提示词来源。

## v0.4.0 - 2026-06-16

+ [新增] 新增网页版Agent Loop模式。
+ [新增] 支持Vercel一键部署。
+ [调整] 移除后端，项目定位为个人画布工具。

## v0.3.0 - 2026-06-15

+ [新增] 新增canvas-agent通过codex操作画布。

## v0.2.5 - 2026-06-08

+ [新增] 新增图片切图功能。
+ [新增] 支持webdav同步数据。
+ [修复] 修复画布文字节点错误问题。

## v0.2.4 - 2026-06-04

+ [新增] 新增图片反推提示词功能。

## v0.2.3 - 2026-06-04

+ [新增] 新增图片蒙版局部修改功能。
+ [优化] 优化配置节点@图片功能。

## v0.2.2 - 2026-06-04

+ [新增] 新增图片放大工具。
+ [优化] 优化图片工具条，增加自定义功能。
+ [修复] 修复端口冲突问题、pg/mysql未初始化问题。

## v0.2.1 - 2026-06-03

+ [新增] 新增文档站点页面。
+ [优化] 优化画布连线交互。
+ [优化] 优化模型选择用户偏好。

## v0.2.0 - 2026-06-01

+ [新增] 支持通过火山方舟AgentPlan接入。
+ [新增] 视频生成支持声音、水印及图片/视频/音频参考输入。
+ [新增] 画布新增音频节点。
+ [优化] 图片/视频素材支持 `图片1`编号注入提示词。

## v0.1.1 - 2026-05-30

+ [新增] 支持New API跳转并自动填入Base URL和API Key配置。

## v0.1.0 - 2026-05-26

+ [优化] 优化我的画布、我的素材导出功能
+ [修复] 修复画布撤销，配置节点等bug问题

## v0.0.9 - 2026-05-26

+ [新增] 新增视频创作台页面。
+ [修复] 修复图片节点size参数传递问题。

## v0.0.8 - 2026-05-24

+ [新增] 新增用户账号与算力点体系，支持账号密码注册登录、Linux.do OAuth。
+ [新增] 管理后台公开配置支持设置模型算力点、支持计费查询。
+ [新增] 画布右上角展示用户算力点余额，生成按钮会展示本次预计消耗算力点。
+ [新增] 新增视频生成节点。

## v0.0.7 - 2026-05-23

+ [新增] 管理后台提示词管理支持多选批量删除。
+ [新增] 新增定义拉取GitHub提示词源功能。
+ [新增] 新增awesome-gpt-image2-prompts提示词来源。
+ [优化] 优化模型下拉选择样式、优化生图编辑设置

## v0.0.6 - 2026-05-22

+ [新增] 管理后台支持配置模型渠道，前端当前无需鉴权即可直接使用后端渠道能力。
+ [优化] 统一整理后端错误提示、AI 代理、图片节点生成与重试、参考图缺失处理等细节。
+ [优化] 后端模型代理路径调整为 OpenAI 风格。

## v0.0.5 - 2026-05-20

+ [新增] 右上角版本号支持点击查看版本更新弹窗，展示当前版本、最新版本和按时间线整理的更新日志。
+ [新增] 设置弹窗支持配置系统提示词，AI 生图、编辑图和文本请求会自动携带。

## v0.0.4 - 2026-05-20

+ [调整] Docker 运行入口改为 Next.js 对外提供页面，`/api/*` 由 Next.js 代理到内部 Go 服务。
+ [修复] 文本复制在局域网 IP 访问时可能失败的问题。

## v0.0.3 - 2026-05-19

+ [修复] 更新 nanoid 依赖并修改 ID 生成方式，防止其他ip无法使用crypto模块导致的ID生成失败问题。

## v0.0.2 - 2026-05-19

+ [新增] 增加生图工作台功能，支持文生图、图生图、查看历史记录，并增加移动端适配。
+ [修复] 画布生成尺寸控件支持选择更多常用比例，并可直接输入自定义比例。
+ [修复] 生成配置节点恢复拖拽操作，避免面板控件拦截整块节点拖动。
+ [文档] 增加 Render 部署说明。

## v0.0.1 - 2026-05-19

+ [新增] 首次开源版本，包含无限画布能力：多画布项目、节点拖拽缩放、连线、小地图、撤销重做、导入导出。
+ [新增] AI 创作能力：支持 OpenAI 兼容接口的文生图、图生图、参考图编辑和文本问答。
+ [新增] 画布助手能力：支持围绕选中节点和上游节点对话、生图，并把结果插回画布。
+ [新增] 提示词库能力：抓取多个 GitHub 开源项目，按案例整理数百个图片提示词。
