# Shell scripts and container configs must keep LF or the Linux kernel fails to
# resolve their shebang ("bash\r: No such file or directory").
*.sh text eol=lf
*.conf text eol=lf
Dockerfile text eol=lf
nginx.conf text eol=lf
